#include <stdio.h> 

 int main() { 
	int len, wid , area;
	 printf("Enter length: ");	 
	 scanf("%i",&len);    
	 printf("Enter width: ");   
	 scanf("%i", &wid);
    	 area =len * wid	 
	 printf("The sum of the three number is %i\n" , len + wid + area );
         return 0;

}
